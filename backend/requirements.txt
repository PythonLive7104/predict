# --- Core ---
Django>=5.0,<5.2
djangorestframework>=3.15

# --- Database (Postgres in prod; SQLite fallback for local dev) ---
psycopg[binary]>=3.2
dj-database-url>=2.2

# --- Config / env ---
django-environ>=0.11

# --- Static serving (admin + DRF browsable API) ---
whitenoise>=6.6
# The WSGI server the Dockerfile CMD and docker-compose both invoke. Absent
# until now, so every container image built fine and then died on
# `sh: gunicorn: not found` — runserver masked it in development.
gunicorn>=23.0

# --- Frontend integration (phase 3: SPA + Telegram Mini App) ---
django-cors-headers>=4.4
djangorestframework-simplejwt>=5.3

# --- Async work: fixture sync, prediction generation, settlement, broadcasts ---
celery>=5.4
redis>=5.0,<6.0
django-celery-beat>=2.7

# --- Telegram bot (aiogram 3: async, FSM, native webhook support) ---
aiogram>=3.13

# --- Data providers (API-Football) + crypto payment webhooks ---
httpx>=0.27
requests>=2.32

# --- Prediction engine ---
# Statistical layer (Poisson / Elo / xG) — numpy+scipy only, no heavy ML deps yet.
numpy>=2.0
scipy>=1.14
# Narrative layer — Claude writes the user-facing rationale over the stat pack.
openai>=3.0
